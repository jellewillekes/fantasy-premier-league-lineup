[project]
name = "fantasy-premier-league-lineup"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.3.4",
    "pandas>=2.3.3",
    "pulp>=3.3.0",
    "requests>=2.32.5",
]

[project.scripts]
run-week = "fpl_engine.run_week:main"

[tool.uv]
package = true